# https://github.com/flightlessmango/MangoHud
# Example: /usr/share/doc/mangohud
legacy_layout=0

time_no_label
time_format = %H:%M

cpu_stats # CPU load
cpu_temp
cpu_mhz
# cpu_power # Need zenergy kernel module. https://github.com/BoukeHaarsma23/zenergy 

gpu_stats # GPU load
gpu_temp
gpu_core_clock
# gpu_power

# ram
vram

# io_read
# io_write

fps
frametime=0


font_size=28
# font_size=20 # 32" 4K
width=240
# width=300
# table_columns=3
background_alpha=0
offset_x=-15
offset_y=-15
text_outline_thickness=0

#toggle_hud=Shift_R+F12
