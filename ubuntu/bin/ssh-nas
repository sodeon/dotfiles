#!/bin/zsh
print -Pn "\e]2;tmux\a" # Set window name to tmux to allow vim-tmux-i3-integration
ssh nas -t "tmux new-session -s user || tmux attach-session -t user" # This request pseudo-terminal. Inside pseudo-terminal, all set title has no effect.
