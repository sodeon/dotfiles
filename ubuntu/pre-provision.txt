#!/bin/bash -ue
#------------------------------------------------------------------------------
# Steps to provision a new OS
#------------------------------------------------------------------------------
: ' 
Install Ubuntu:
    Windows: Use "Rufus" to copy image to USB flash drive
    Linux: $burn-iso # copy image to USB flash drive
    Disable fast boot in BIOS
    Use flash drive to install Ubuntu afresh

Provision:
    Install git
    Get dotfiles and run provision script
        $git clone https://github.com/sodeon/dotfiles ~/dotfiles
        $cd ~/dotfiles/ubuntu && ./provision.sh
'

