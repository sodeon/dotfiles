#---------------------------------------------------------------------------------------------------
# NOTE:
#     When paired with xdotool, system will see both the triggering shortcut and triggered shortcut.
#     Therefore, add keyup to untrigger the triggering shortcut first
#
# Reload rc file: xbindkeys --poll-rc
# Interactive key: xbindkeys --key
#                  xbindkeys --multikey
#
# Mod3: Caps_Lock
# Mod4: Windows
#---------------------------------------------------------------------------------------------------
# Mouse button remap
"xte 'keydown Control_L' 'key Page_Up' 'keyup Control_L'"
b:6
"xte 'keydown Control_L' 'key Page_Down' 'keyup Control_L'"
b:7
"xte 'keydown Control_L' 'key w' 'keyup Control_L'"
b:8
"xte 'keydown Alt_L' 'key Left' 'keyup Alt_L'"
b:9

# Volume control
# "amixer -D pulse set Master 5%+ && pkill -RTMIN+10 i3blocks"
# Control_R
# "amixer -D pulse set Master 5%- && pkill -RTMIN+10 i3blocks"
# Alt_R


#---------------------------------------------------------------------------------
# Autohotkey porting
#---------------------------------------------------------------------------------
# "xdotool keyup p key ctrl+w"
# Mod3 + p
# "xdotool keyup u key ctrl+u"
# Mod3 + u


# Brightness/color temperature
# https://github.com/haikarainen/light
# TODO: app aware brightness
# "light -U 5  && pkill -RTMIN+11 i3blocks"
# Mod3 + Down
# "light -A 5  && pkill -RTMIN+11 i3blocks"
# Mod3 + Up
# "light -S 25 && pkill -RTMIN+11 i3blocks"
# Mod3 + b
# no need to have a shortcut to change resolution
# Mod3 + r

