 # Mouse button remap
 "xte 'keydown Control_L' 'key Page_Up' 'keyup Control_L'"
 b:6
 "xte 'keydown Control_L' 'key Page_Down' 'keyup Control_L'"
 b:7
 "xte 'keydown Control_L' 'key w' 'keyup Control_L'"
 b:8
 "xte 'keydown Alt_L' 'key Left' 'keyup Alt_L'"
 b:9


" Media control (Mod4: window)
"cmus-remote -n"
Control+Mod4 + Down
"cmus-remote -r"
Control+Mod4 + Up
"cmus-remote -u"
Control+Mod4 + k


# Volume control
"amixer -D pulse sset Master 5%+"
Control_R
"amixer -D pulse sset Master 5%-"
Alt_R


#---------------------------------------------------------------------------------
# Doesn't work
#---------------------------------------------------------------------------------
# Can only control the first audio device. Use xmodmap instead
#"pactl set-sink-volume 0 +6500"
#Control_R
#"pactl set-sink-volume 0 -6500"
#Alt_R

# For whatever reason, the sleep trick before sending key is necessary or volume won't change
# This approach is better than above, but sometimes doesn't work
#"sleep 0.1; xte 'key XF86AudioRaiseVolume'"
#Control_R
#"sleep 0.1; xte 'key XF86AudioLowerVolume'"
#Alt_R

# Media control (doesn't work)
#"cmus-remote -u"
#XF86AudioPlay
#"cmus-remote -n"
#XF86AudioNext
#"cmus-remote -r"
#XF86AudioPrev

# This works but the device numbering is not reliable
#   sink 1: USB DAC
#   sink 2: NVIDIA HDMI
#   Determine which audio device is in use and increase/decrease its volume
#"((pactl info | grep hdmi) && pactl set-sink-volume 2 +5%) || pactl set-sink-volume 1 +5%"
#"((pactl info | grep hdmi) && pactl set-sink-volume 2 -5%) || pactl set-sink-volume 1 -5%"
